# ChessGame
A simple command-line chess game, featuring turn-based gameplay, legal move validation, and check/checkmate detection.

Player vs. AI and AI vs. AI are WIP.

`make run` to compile and run the program.
